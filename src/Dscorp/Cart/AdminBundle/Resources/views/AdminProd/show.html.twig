{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AdminProd</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Caracteristicas</th>
                <td>{{ entity.caracteristicas }}</td>
            </tr>
            <tr>
                <th>Preciou</th>
                <td>{{ entity.precioU }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('adminprod') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('adminprod_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
