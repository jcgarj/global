{% extends '::base.html.twig' %}

{% block body -%}
    <h1>cart creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cart') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
