{% extends '::base.html.twig' %}

{% block body -%}
    <h1>cart list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Estatus</th>
                <th>Llave</th>
                <th>Datecart</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cart_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.estatus }}</td>
                <td>{{ entity.llave }}</td>
                <td>{% if entity.dateCart %}{{ entity.dateCart|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cart_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cart_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cart_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
